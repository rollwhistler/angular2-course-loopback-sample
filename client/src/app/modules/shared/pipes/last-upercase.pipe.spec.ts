import { LastUpercasePipe } from './last-upercase.pipe';

describe('LastUpercasePipe', () => {
  it('create an instance', () => {
    const pipe = new LastUpercasePipe();
    expect(pipe).toBeTruthy();
  });
});
